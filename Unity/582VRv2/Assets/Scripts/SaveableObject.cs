using UnityEngine;

public class SaveableObject : MonoBehaviour
{
    public bool isSaveable = true; //Helper class, attach this script to objects we want to save the position of instead of using tags
}
